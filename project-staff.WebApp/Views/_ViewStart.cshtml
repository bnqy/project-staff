@{
    var path = Context.Request.Path.Value;
    if ((User?.Identity?.IsAuthenticated ?? false) && path != "/Account/Login" && path != "/" && path != "/Account/Register")
    {
        Layout = "_Layout";
    }
    else
    {
        Layout = "_LayoutGuest";
    }
}